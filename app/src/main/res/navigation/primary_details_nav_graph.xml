<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/note_list_fragment">

    <fragment
        android:id="@+id/note_list_fragment"
        android:name="com.example.deardiary.NoteListFragment"
        android:label="NoteListFragment">
        <action
            android:id="@+id/show_note_detail"
            app:destination="@id/note_detail_fragment" />
    </fragment>
    <fragment
        android:id="@+id/note_detail_fragment"
        android:name="com.example.deardiary.NoteDetailFragment"
        android:label="note_detail"
        tools:layout="@layout/fragment_note_detail">
        <argument
            android:name="item_id"
            android:defaultValue=""
            app:argType="string" />
    </fragment>
</navigation>